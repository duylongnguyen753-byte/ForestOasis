-- ========================================================
-- FOREST HUB | WINDUI LOADER (NOTIFICATION LINK)
-- ========================================================

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "🌿 ForestHUB | System Loader",
    Icon = "leaf",
    Author = "✨ Official Team Forest ✨",
    Folder = "ForestHubConfigs",
    Size = UDim2.fromOffset(500, 350),
    Transparent = true,
    Theme = "Plant", -- Sử dụng theme xanh lá cây chủ đạo
    SideBarWidth = 150,
    HasOutline = true
})

local Tab = Window:Tab({ Title = "📢 Thông Báo Script", Icon = "megaphone" })

Tab:Paragraph({
    Title = "⚠️ THÔNG BÁO QUAN TRỌNG",
    Desc = "• Script đã thay đổi!\n• Vui lòng liên hệ @m4c_xd hoặc vào nhóm để nhận lại script mới nhất."
})

Tab:Button({
    Title = "📋 Copy thông báo",
    Callback = function()
        if setclipboard then
            setclipboard("Script đã thay đổi, vui lòng liên hệ @m4c_xd hoặc vào nhóm để nhận lại script!")
            WindUI:Notify({
                Title = "✨ Thành công",
                Content = "Đã sao chép nội dung vào bộ nhớ tạm!",
                Duration = 3
            })
        end
    end
})

-- Thông báo nổi khi vừa bật giao diện
WindUI:Notify({
    Title = "🌿 ForestHUB",
    Content = "Script đã thay đổi, vui lòng liên hệ @m4c_xd hoặc vào nhóm để nhận lại script!",
    Duration = 8
})
